import { RotatableDirective } from './rotatable.directive';

describe('RotatableDirective', () => {
  it('should create an instance', () => {
    const directive = new RotatableDirective();
    expect(directive).toBeTruthy();
  });
});
